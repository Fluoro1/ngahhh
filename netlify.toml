[build]
  # Leave command empty because there is no package.json to run
  command = ""
  # Set publish to "." to serve files (like index.html) from the root
  publish = "."

[[redirects]]
  # Keeps your SPA-style routing working if applicable
  from = "/*"
  to = "/index.html"
  status = 200
